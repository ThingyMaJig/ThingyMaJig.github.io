source 'https://rubygems.org'

gem 'gsl'

group :jekyll_plugins do
    gem 'github-pages'

    # Required for importing
    gem 'jekyll-import'
    gem 'sequel'
    gem 'mysql'

    gem "jekyll-assets"
end

gem 'bootstrap-sass'